const color  = require('./color').default;
const color2 = require('./color2').default;
const csb    = require('./coloredBlocks').default;

const Transformations = {
    color: color,
    color2: color2,
    ColorSampleBlocks: csb,
}

export default Transformations;